	/// corroborar si la secuencia termino

		//(se esta haciendo una transicion?)
		if(fadeInn!=noone){
		
			room_goto(Intro);
	
			fadeInn=noone;// limpiamos la variable
		}
		
			//(se esta haciendo una transicion?)
		if(fadeInnn!=noone){
		
			room_goto(Controles);
	
			fadeInn=noone;// limpiamos la variable
		}
		
		